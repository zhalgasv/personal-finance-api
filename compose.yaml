services:
  postgres:
    image: 'postgres:latest'
    environment:
      POSTGRES_DB: 'personal_finance'
      POSTGRES_PASSWORD: 'secretpassword'
      POSTGRES_USER: 'adil'
    ports:
      - '5433:5432'